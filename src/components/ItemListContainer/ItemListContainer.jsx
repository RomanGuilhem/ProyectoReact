const ItemListContainer = ({greeting}) => {
    return(
        <div style={{fontSize: "50px", height: "90vh", width: "100vw", display: "flex", justifyContent: "center", color: "whitesmoke"}}>{greeting}</div>
    );
};

export default ItemListContainer